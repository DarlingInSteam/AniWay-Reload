version: "3.9"

networks:
  monitoring-net:
    driver: bridge
  aniway-network:
    external: true

volumes:
  prometheus-data:
  grafana-data:
  alertmanager-data:

services:
  prometheus:
    image: prom/prometheus:v2.53.0
    container_name: aniway-prometheus
    restart: unless-stopped
    user: "0:0"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus-data:/prometheus
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.enable-lifecycle"
      - "--web.external-url=${PROMETHEUS_EXTERNAL_URL:?PROMETHEUS_EXTERNAL_URL not set}"
    ports:
      - "9090:9090"
    networks:
      monitoring-net: {}
      aniway-network:
        aliases:
          - aniway-prometheus
    labels:
      org.label-schema.group: "monitoring"

  grafana:
    image: grafana/grafana:10.4.3
    container_name: aniway-grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=${GF_SECURITY_ADMIN_USER:?GF_SECURITY_ADMIN_USER not set}
      - GF_SECURITY_ADMIN_PASSWORD=${GF_SECURITY_ADMIN_PASSWORD:?GF_SECURITY_ADMIN_PASSWORD not set}
      - GF_USERS_DEFAULT_THEME=dark
      - GF_INSTALL_PLUGINS=grafana-piechart-panel,grafana-worldmap-panel
      - GF_PATHS_PROVISIONING=/etc/grafana/provisioning
      - GF_AUTH_ANONYMOUS_ENABLED=false
      - GF_SERVER_ROOT_URL=${GF_SERVER_ROOT_URL:?GF_SERVER_ROOT_URL not set}
      - GF_SERVER_DOMAIN=${GF_SERVER_DOMAIN:?GF_SERVER_DOMAIN not set}
      - GF_SERVER_SERVE_FROM_SUB_PATH=${GF_SERVER_SERVE_FROM_SUB_PATH:?GF_SERVER_SERVE_FROM_SUB_PATH not set}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3000:3000"
    networks:
      monitoring-net: {}
      aniway-network:
        aliases:
          - aniway-grafana
    depends_on:
      - prometheus
    labels:
      org.label-schema.group: "monitoring"

  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: aniway-alertmanager
    restart: unless-stopped
    entrypoint:
      - /bin/sh
      - /etc/alertmanager/render-config.sh
    command:
      - "--config.file=/etc/alertmanager/alertmanager.yml"
      - "--storage.path=/alertmanager"
      - "--cluster.listen-address=0.0.0.0:9094"
      - "--web.external-url=${ALERTMANAGER_EXTERNAL_URL:?ALERTMANAGER_EXTERNAL_URL not set}"
    volumes:
      - ./alertmanager/alertmanager.yml.tpl:/etc/alertmanager/alertmanager.yml.tpl:ro
      - ./alertmanager/render-config.sh:/etc/alertmanager/render-config.sh:ro
      - ./alertmanager/templates:/etc/alertmanager/templates:ro
      - alertmanager-data:/alertmanager
    environment:
      - ALERTMANAGER_TELEGRAM_BOT_TOKEN=${ALERTMANAGER_TELEGRAM_BOT_TOKEN:?set-telegram-bot-token}
      - ALERTMANAGER_TELEGRAM_CHAT_ID=${ALERTMANAGER_TELEGRAM_CHAT_ID:?set-telegram-chat-id}
      - ALERTMANAGER_EMAIL_TO=${ALERTMANAGER_EMAIL_TO:?set-alert-email}
      - ALERTMANAGER_EMAIL_FROM=${ALERTMANAGER_EMAIL_FROM:?set-alert-from}
      - ALERTMANAGER_SMTP_HOST=${ALERTMANAGER_SMTP_HOST:?set-smtp-host}
      - ALERTMANAGER_SMTP_PORT=${ALERTMANAGER_SMTP_PORT:?ALERTMANAGER_SMTP_PORT not set}
      - ALERTMANAGER_SMTP_USERNAME=${ALERTMANAGER_SMTP_USERNAME:?set-smtp-username}
      - ALERTMANAGER_SMTP_PASSWORD=${ALERTMANAGER_SMTP_PASSWORD:?set-smtp-password}
    ports:
      - "9093:9093"
    networks:
      monitoring-net: {}
      aniway-network:
        aliases:
          - aniway-alertmanager
    labels:
      org.label-schema.group: "monitoring"

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.49.1
    container_name: aniway-cadvisor
    restart: unless-stopped
    privileged: true
    command:
      - "--enable_metrics=cpu,percpu,process,network,disk,diskIO"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /var/lib/docker/:/var/lib/docker:ro
    ports:
      - "8088:8080"
    networks:
      - monitoring-net
      - aniway-network
    labels:
      org.label-schema.group: "monitoring"
      prometheus.scrape: "true"
      prometheus.job: "cadvisor"
      prometheus.port: "8080"

  node-exporter:
    image: prom/node-exporter:v1.7.0
    container_name: aniway-node-exporter
    restart: unless-stopped
    command:
      - "--path.rootfs=/host"
    pid: host
    volumes:
      - /:/host:ro,rslave
    ports:
      - "9100:9100"
    networks:
      - monitoring-net
      - aniway-network
    labels:
      org.label-schema.group: "monitoring"
      prometheus.scrape: "true"
      prometheus.job: "node"
      prometheus.port: "9100"

  postgres-exporter-auth:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-auth
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${AUTH_DB_URI:?AUTH_DB_URI not set}
      - DATA_SOURCE_USER=${AUTH_DB_USER:?AUTH_DB_USER not set}
      - DATA_SOURCE_PASS=${AUTH_DB_PASS:?AUTH_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "auth-postgres"

  postgres-exporter-manga:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-manga
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${MANGA_DB_URI:?MANGA_DB_URI not set}
      - DATA_SOURCE_USER=${MANGA_DB_USER:?MANGA_DB_USER not set}
      - DATA_SOURCE_PASS=${MANGA_DB_PASS:?MANGA_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "manga-postgres"

  postgres-exporter-comment:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-comment
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${COMMENT_DB_URI:?COMMENT_DB_URI not set}
      - DATA_SOURCE_USER=${COMMENT_DB_USER:?COMMENT_DB_USER not set}
      - DATA_SOURCE_PASS=${COMMENT_DB_PASS:?COMMENT_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "comment-postgres"

  postgres-exporter-friend:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-friend
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${FRIEND_DB_URI:?FRIEND_DB_URI not set}
      - DATA_SOURCE_USER=${FRIEND_DB_USER:?FRIEND_DB_USER not set}
      - DATA_SOURCE_PASS=${FRIEND_DB_PASS:?FRIEND_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "friend-postgres"

  postgres-exporter-chapter:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-chapter
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${CHAPTER_DB_URI:?CHAPTER_DB_URI not set}
      - DATA_SOURCE_USER=${CHAPTER_DB_USER:?CHAPTER_DB_USER not set}
      - DATA_SOURCE_PASS=${CHAPTER_DB_PASS:?CHAPTER_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "chapter-postgres"

  postgres-exporter-image:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-image
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${IMAGE_DB_URI:?IMAGE_DB_URI not set}
      - DATA_SOURCE_USER=${IMAGE_DB_USER:?IMAGE_DB_USER not set}
      - DATA_SOURCE_PASS=${IMAGE_DB_PASS:?IMAGE_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
  prometheus.instance: "${IMAGE_DB_INSTANCE:?IMAGE_DB_INSTANCE not set}"

  postgres-exporter-moment:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-moment
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${MOMENT_DB_URI:?MOMENT_DB_URI not set}
      - DATA_SOURCE_USER=${MOMENT_DB_USER:?MOMENT_DB_USER not set}
      - DATA_SOURCE_PASS=${MOMENT_DB_PASS:?MOMENT_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "moment-postgres"

  postgres-exporter-forum:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-forum
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${FORUM_DB_URI:?FORUM_DB_URI not set}
      - DATA_SOURCE_USER=${FORUM_DB_USER:?FORUM_DB_USER not set}
      - DATA_SOURCE_PASS=${FORUM_DB_PASS:?FORUM_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "forum-postgres"

  postgres-exporter-notification:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-notification
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${NOTIFICATION_DB_URI:?NOTIFICATION_DB_URI not set}
      - DATA_SOURCE_USER=${NOTIFICATION_DB_USER:?NOTIFICATION_DB_USER not set}
      - DATA_SOURCE_PASS=${NOTIFICATION_DB_PASS:?NOTIFICATION_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "notification-postgres"

  postgres-exporter-message:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-message
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${MESSAGE_DB_URI:?MESSAGE_DB_URI not set}
      - DATA_SOURCE_USER=${MESSAGE_DB_USER:?MESSAGE_DB_USER not set}
      - DATA_SOURCE_PASS=${MESSAGE_DB_PASS:?MESSAGE_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "message-postgres"

  postgres-exporter-post:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-post
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${POST_DB_URI:?POST_DB_URI not set}
      - DATA_SOURCE_USER=${POST_DB_USER:?POST_DB_USER not set}
      - DATA_SOURCE_PASS=${POST_DB_PASS:?POST_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "post-postgres"

  postgres-exporter-level:
    image: quay.io/prometheuscommunity/postgres-exporter:v0.15.0
    container_name: aniway-postgres-exporter-level
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=${LEVEL_DB_URI:?LEVEL_DB_URI not set}
      - DATA_SOURCE_USER=${LEVEL_DB_USER:?LEVEL_DB_USER not set}
      - DATA_SOURCE_PASS=${LEVEL_DB_PASS:?LEVEL_DB_PASS not set}
      - PG_EXPORTER_AUTO_DISCOVER_DATABASES=true
    networks:
      - monitoring-net
      - aniway-network
    labels:
      prometheus.scrape: "true"
      prometheus.job: "postgres"
      prometheus.port: "9187"
      prometheus.instance: "level-postgres"
